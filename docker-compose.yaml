services:
  valkey:
    image: valkey/valkey:8-alpine
    restart: unless-stopped
    command: ["valkey-server", "--requirepass", "${VALKEY_REQUIREPASS:?VALKEY_REQUIREPASS must be set}", "--appendonly", "yes"]
    environment:
      VALKEY_REQUIREPASS: ${VALKEY_REQUIREPASS:?VALKEY_REQUIREPASS must be set}
    volumes:
      - valkey-data:/data
    # No ports published by default. For local standalone access, uncomment:
    # ports:
    #   - "6379:6379"
    # Security hardening (OWASP Docker Security Cheat Sheet) — see docker-compose.coolify.yaml
    # for why each of these values is exactly what it is, verified against a real container.
    # Deliberately NOT using --aclfile here, unlike the Coolify variant: this file is for
    # disposable local dev (`docker compose down -v` is expected between sessions), and an
    # aclfile requires a pre-existing seed file or the container refuses to start — adding
    # that bootstrap step would work against the "cp .env.example .env && docker compose up"
    # zero-friction goal. The Coolify variant needs it because it auto-redeploys on every push
    # to this repo (even docs-only) and that was silently wiping ACL SETUSER grants, which
    # matters for a long-lived shared instance in a way it doesn't for a throwaway local one.
    # user: "999:1000" (valkey:valkey, baked into the image) — the image's entrypoint drops
    # from root to this user via `setpriv`, which needs SETUID/SETGID; cap_drop: ALL removes
    # those, so the container must start as this user directly instead. /data is already
    # valkey-owned and world-writable (1777) in the image, so this works against a fresh
    # named volume with no extra chown step.
    user: "999:1000"
    cap_drop:
      - ALL
    security_opt:
      - no-new-privileges:true
    read_only: true
    tmpfs:
      - /tmp
    mem_limit: 512m
    cpus: 1.0
    pids_limit: 200
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
    healthcheck:
      test: ["CMD-SHELL", "valkey-cli --no-auth-warning -a \"$$VALKEY_REQUIREPASS\" ping | grep -q PONG"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 10s

volumes:
  valkey-data:
